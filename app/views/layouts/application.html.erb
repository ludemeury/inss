<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <header class="bg-primary text-white text-center py-3">
      <h1>Desconto INSS</h1>
    </header>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <% if authenticated? %>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex justify-content-center gap-4">
            <li class="nav-item">
              <%= link_to "Proponentes", proponents_path, class: "nav-link px-3 py-2 rounded-3 text-dark active" %>
            </li>
            <li class="nav-item">
              <%= link_to "Dashboards", dashboards_path, class: "nav-link px-3 py-2 rounded-3 text-dark" %>
            </li>
            <li class="nav-item">
              <%= button_to "Sair", session_path, method: :delete, class: "nav-link px-3 py-2 rounded-3 text-dark" if authenticated? %>
            </li>
          </ul>
        <% end %>
      </div>
    </nav>

    <main class="flex-grow-1 container-fluid my-4">
      <div id="content" class="px-4">
        <%= yield %>
      </div>
    </main>

    <footer class="bg-dark text-white text-center py-3">
      <p>&copy; 2024 Desconto INSS.</p>
    </footer>
  </body>
</html>
